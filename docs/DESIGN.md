# DESIGN.md — krx-signal-briefing

> **대상: 아침 브리핑 메일 본문.** 이 프로젝트에 화면은 없다. 사용자가 보는 유일한 표면이 메일이므로,
> 워크스페이스 CLAUDE.md의 "UI 디자인 사전 합의 원칙"을 메일 본문에 적용한다.
> 시안 캔버스: <https://claude.ai/code/artifact/10587f88-b64f-4e64-af28-715bdf72d503>
> 합의: 2026-08-29 (사용자 — 추천안 채택). 구현: `briefing/render.py`, 계약 테스트: `tests/test_render.py`.

---

## 1. 왜 다시 짰나 — 첫 판의 문제 (2026-08-29 실측)

첫 판은 `text()`를 `<pre>`로 감싼 한 덩어리였다. 실제로 도착한 메일에서 네 가지가 드러났다.

| # | 문제 | 결과 |
|---|------|------|
| ① | 공시마다 URL이 **제목 아래 별도 줄**로 나왔다 | 줄 수가 두 배. 그 줄은 전부 노이즈 |
| ② | 조건·공시·보조 신호·뉴스가 **같은 무게**로 보였다 | 무엇이 중요한지 알 수 없다 |
| ③ | 15종목이 **평등하게** 나열됐다 | 🔴 2건이 그 사이에 묻힌다 |
| ④ | 스캔 경로가 없다 | 처음부터 끝까지 읽어야 오늘 볼 것이 있는지 안다 |

본문 18,012자. 아침에 읽는 메일로는 길다.

## 2. 시각 어휘 — 상위 메일에서 잇는다

`krx-signal-alerts`의 알림 메일과 **한 아침에 나란히 도착한다.** 다른 팔레트를 쓰면 남의 메일처럼 보인다.
`alerts/render.py`의 값을 그대로 가져왔다.

| 역할 | 값 | 출처 |
|------|-----|------|
| 서체 | `-apple-system, 'Apple SD Gothic Neo', 'Malgun Gothic', sans-serif` | 상위 동일 |
| 본문 글자 | `#121829` · 보조 `#3D465F` · 흐림 `#626D8A` · 더 흐림 `#8C96AE` | 상위 동일 |
| 상승 / 하락 | `#C9283E` / `#1F63A8` | 상위 동일 (국내 관행) |
| 바탕 / 띠 / 선 | `#EEF1F7` / `#F5F7FB` / `#D8DEEB`·`#F0F3F9` | 신규 (상위 톤에서 파생) |
| 🔴 등급 | 점 `#C9283E` · 카드 바탕 `#FDF3F4` · 테두리 `#E7B8BF` · 글자 `#8A1F2F` | 신규 |
| 🟡 등급 | 점 `#B76E00` · 바탕 `#FEF7EC` · 테두리 `#E8D2A8` · 글자 `#7A4A00` | 신규 (같은 채도·명도, 색상만 이동) |

폭 600px 고정. 휴대폰에서는 표가 가로로 눌리지 않게 열 수를 그대로 두고 글자만 줄인다.

## 3. 합의된 구조 (추천안 채택)

```
머리      날짜 · 한 문장 · 등급 칩
인덱스 표  전 종목: ● 종목 / 종가 / 등락 / 공시(위험·전체) / 시총    ← 한 번에 훑는 유일한 자리
구역 ●    위험 유형이 확인된 종목  N
  카드      머리 밴드(등급색) · 한 줄 판정
            공시 표 — 위험 유형 먼저, 제목이 곧 링크
            신호 조건 표 — 상위 evidence 그대로
            참고값 줄 — 시총 · 거래대금 · 이상 점수 · ⚠ 생략
구역 ○    확인된 위험 유형이 없는 종목  N
  압축 카드  조건은 "5/5" 한 줄 · 공시 3건 · 뉴스 3건 · 참고값
꼬리      한계 문구 (R7 — 절대 지우지 않는다)
```

**검토한 다른 두 안** (캔버스에 남아 있다)

| 안 | 무엇 | 채택하지 않은 이유 |
|---|------|--------------------|
| A · 카드 단독 | 표 없이 넉넉한 카드만 | 15종목을 한눈에 볼 방법이 없다 |
| B · 전면 표 | 카드 없이 전부 표 행 | 신호 조건 5줄이 들어갈 자리가 없다 |

## 4. 확정한 결정

| # | 결정 | 이유 |
|---|------|------|
| **G1** | **제목 자체가 링크.** URL을 본문에 다시 적지 않는다 | 줄 수 절반. 첫 판 문제 ① |
| **G2** | **맨 위 인덱스 표.** 전 종목이 여기 남는다 | 스캔 경로. 첫 판 문제 ③④ |
| **G3** | **본문에 이모지를 쓰지 않는다.** 등급은 색 점·색 칩 | 클라이언트마다 다른 그림이 온다. 제목(F8)의 🔴은 유지 — 받은편지함에서 눈에 띄어야 한다 |
| **G4** | **위험 없는 종목은 압축 카드** — 조건 한 줄, 공시 3건, 뉴스 3건 | 18,012자 → 6,539자 |
| **G5** | **되풀이되는 치수는 `<style>` 클래스, 뜻이 있는 색·굵기는 inline** | `<style>`을 지우는 클라이언트에서도 등급 색은 남아야 한다 |
| **G6** | **배치는 `<table>`만** | flex·grid는 안드로이드 Gmail에서 무너진다 |
| **G7** | **평문 대체본은 위험 종목만 펼친다.** 나머지는 한 줄 | 두 벌을 다 펴면 Gmail 클리핑 한계를 넘는다 (§5) |

## 5. 길이 예산 — 설계의 핵심 (SPEC F7b · N15)

메일 한 통이 **102,400 bytes**를 넘으면 Gmail이 `[메시지 전체 보기]`로 잘라낸다.
잘리면 꼬리의 한계 문구(R7)까지 사라지므로, 이것은 미관이 아니라 규정 문제다.

| 판 | 크기 | 무엇이 달랐나 |
|---|------|---------------|
| 첫 판 (`<pre>`) | 본문 18,012자 | 잘리지는 않았으나 읽히지 않았다 |
| 시안 1차 (전부 inline) | **149,971 bytes — 잘림** | 스타일 문자열이 줄마다 되풀이 |
| 클래스 접기 후 | 111,339 bytes — 여전히 잘림 | 평문이 15종목을 다 펼침 |
| **확정** | **78,269 bytes · 여유 24%** | 클래스 + 평문 한 줄 요약 |

**접히는 것과 접히지 않는 것**

| 접는다 (예산 초과 시) | 접지 않는다 |
|---|---|
| 위험 없는 종목의 압축 카드 (뒤에서부터) | 인덱스 표 — 전 종목 |
| 평문의 위험 없는 종목 상세 | 위험 종목 카드와 그 공시 전부 |
| 공시·뉴스 목록의 꼬리 | 한계 문구 · 요약 실패 표기 |

잘린 사실은 언제나 드러낸다 — `외 N건`, `나머지 N종목은 위 「한눈에 보기」 표에 있습니다`.

## 6. 문구 경계 (SPEC N1) — 디자인이 흔들면 안 되는 것

카드·칩·색이 늘어도 **사실 나열은 되고 판단은 안 된다**는 규칙은 그대로다.

- 등급 칩은 `위험 유형 확인 2`이지 `위험 2`가 아니다. 색은 강조일 뿐 판단이 아니다.
- 머리 밴드의 한 줄은 규칙 이름을 한국어로 적는다 (`전환사채 발행결정 1건`) — `오버행 주의`가 아니다.
- 공시·뉴스 제목은 **원문**이라 금지어가 들어 있어도 그대로 싣는다. 우리가 쓴 문장만 검사한다.

## 7. 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-08-29 | 최초 작성. 첫 실메일의 문제 4가지 → 3안 시안 → **추천안(인덱스 표 + 카드) 합의** → 구현·실측. G1~G7 확정 |
